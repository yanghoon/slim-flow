apiVersion: v1
kind: Service
metadata:
  name: {{ include "flink-compose.fullname" . }}
  labels:
    {{- include "flink-compose.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "flink-compose.selectorLabels" . | nindent 4 }}
